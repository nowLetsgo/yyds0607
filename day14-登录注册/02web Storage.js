/* 
    web Storage:
        - web Storage是html5提供的一个新特性，主要是用来做本地存储的，解决了cookie在做本地存储时的一些缺点
        - web Stroage分为两种，一种是localStorage(永久存储)，一种是sessionStorage(临时存储)

        - 优点：
            - 解决了cookie的存储空间问题，web Storage可以达到5M左右，相当于客户端本地的5M的数据库
            - 不会随着http的请求发送
            - 接口简单

        - 局限：
            - 低版本ie不支持


*/